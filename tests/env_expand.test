# Simple replacements
echo $HOME
cd $HOME
echo $UNKNOWNVARHAHA
echo $_SALUT
echo $?SALUT

# Expanding in quoted strings
echo "$USER"
echo '$USER'

# what the fuck
echo $"" "$" '$' $a '$a' "$a" $"a" $'a' "'$a'" '"$a"' '"'$a'"' "'"$a"'" '$'a "$"a
echo "$" '$' $a '$a' "$a" "'$a'" '"$a"' '"'$a'"' "'"$a"'" '$'a "$"a
